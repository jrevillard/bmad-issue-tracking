diff --git a/_bmad/bmm/config.yaml b/_bmad/bmm/config.yaml
index d6200549..faa1bada 100644
--- a/_bmad/bmm/config.yaml
+++ b/_bmad/bmm/config.yaml
@@ -14,3 +14,17 @@ user_name: Jerome
 communication_language: French
 document_output_language: English
 output_folder: "{project-root}/_bmad-output"
+
+# Issue Tracking Configuration
+# Mirrors sprint tracking to GitLab Issues (glab) or GitHub Issues (gh).
+# Fallback to file-system tracking when the issue tracker is unavailable.
+issue_tracking:
+  enabled: true
+  # Platform: "gitlab" or "github". Read by sync/link tasks to pick the CLI.
+  platform: gitlab
+  # PRD key is read from prd.md frontmatter (field: prd_key) — not configured here
+  # Optional: link to an existing issue as PRD parent (skip auto-creation)
+  # prd_parent_issue: 218
