diff --git a/_bmad/bmm/workflows/4-implementation/sprint-planning/instructions.md b/_bmad/bmm/workflows/4-implementation/sprint-planning/instructions.md
index 316d2fec..5b2c0f27 100644
--- a/_bmad/bmm/workflows/4-implementation/sprint-planning/instructions.md
+++ b/_bmad/bmm/workflows/4-implementation/sprint-planning/instructions.md
@@ -180,6 +180,20 @@ development_status:
 
 </step>
 
+<step n="6" goal="Sync to Issue tracker Issues (if Issue tracker tracking enabled)">
+<check if="issue_tracking is enabled in {config_source}">
+  <action>Read the shared sync task at `_bmad/_config/custom/bmad-bmm-issue-sync.md` and follow Steps 1-4 (detect project, ensure labels, create PRD issue, sync issues) using the platform CLI (glab or gh) detected in the sync task</action>
+
+  <check if="Issue tracker is NOT reachable at any point">
+    <output>Issue tracker unavailable — using file-system tracking. Run sync on next successful connection.</output>
+  </check>
+</check>
+
+<check if="issue_tracking is NOT enabled">
+  <note>Issue tracker tracking is disabled in config. Using file-system tracking only.</note>
+</check>
+</step>
+
 </workflow>
 
 ## Additional Documentation
