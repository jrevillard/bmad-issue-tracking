diff --git a/_bmad/bmm/workflows/4-implementation/sprint-status/instructions.md b/_bmad/bmm/workflows/4-implementation/sprint-status/instructions.md
index 4182e1f2..9701bb80 100644
--- a/_bmad/bmm/workflows/4-implementation/sprint-status/instructions.md
+++ b/_bmad/bmm/workflows/4-implementation/sprint-status/instructions.md
@@ -42,6 +42,16 @@ Run `/bmad:bmm:workflows:sprint-planning` to generate it, then rerun sprint-stat
   - Retrospectives: keys ending with "-retrospective"
   - Stories: everything else (e.g., 1-2-login-form)
   <action>Map legacy story status "drafted" → "ready-for-dev"</action>
+
+  <!-- If Issue tracker tracking is enabled, also reconcile with Issue tracker -->
+  <check if="issue_tracking is enabled in config">
+    <action>Detect Issue tracker project by following Step 1 of the shared sync task at `_bmad/_config/custom/bmad-bmm-issue-sync.md`</action>
+    <check if="Issue tracker is reachable">
+      <action>Read the shared sync task at `_bmad/_config/custom/bmad-bmm-issue-sync.md` and follow Steps 2-4 (Ensure labels, Sync issues) to sync any differences between yaml and Issue tracker</action>
+    </check>
+    <check if="Issue tracker is NOT reachable">
+      <note>Issue tracker unavailable — using sprint-status.yaml as local tracking source</note>
+    </check>
+  </check>
   <action>Count story statuses: backlog, ready-for-dev, in-progress, review, done</action>
   <action>Map legacy epic status "contexted" → "in-progress"</action>
   <action>Count epic statuses: backlog, in-progress, done</action>
