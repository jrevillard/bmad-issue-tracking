diff --git a/_bmad/bmm/workflows/4-implementation/code-review/instructions.xml b/_bmad/bmm/workflows/4-implementation/code-review/instructions.xml
index e5649559..a63e883d 100644
--- a/_bmad/bmm/workflows/4-implementation/code-review/instructions.xml
+++ b/_bmad/bmm/workflows/4-implementation/code-review/instructions.xml
@@ -214,6 +214,13 @@
       <output>ℹ️ Story status updated (no sprint tracking configured)</output>
     </check>

+    <!-- Sync to Issue tracker if tracking enabled -->
+    <check if="issue_tracking is enabled in config">
+      <action>Read the shared sync task at `_bmad/_config/custom/bmad-bmm-issue-sync.md` and follow Steps 1-4 to sync the story's review status ({{new_status}}) to the issue tracker. Use the platform CLI detected in Step 1 (glab or gh).</action>
+      <check if="Issue tracker is NOT reachable at any point">
+        <note>Issue tracker unavailable — status will be synced on next connection</note>
+      </check>
+    </check>
+
      <output>**✅ Review Complete!**

        **Story Status:** {{new_status}}
